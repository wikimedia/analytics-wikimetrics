from nose.tools import *

def test_job_maps_correctly_to_db():
    assert_true(False, "Cohort Metrics Job does not map properly")
    assert_true(False, "Query Job does not map properly")

def test_user_maps_correctly_to_db():
    assert_true(True, "User does not map properly")

def test_wikiuser_maps_correctly_to_db():
    assert_true(True, "WikiUser does not map properly")

def test_cohort_maps_correctly_to_db():
    assert_true(True, "Cohort does not map properly")
